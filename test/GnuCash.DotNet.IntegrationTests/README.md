# GnuCash.DotNet.IntegrationTests

Integration tests in this folder should require an installed official GnuCash for Windows.

Keep them opt-in until the SDK can reliably detect the local installation path and bridge compatibility.